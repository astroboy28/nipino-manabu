<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/res/xml/network_security_config.xml -->
<!-- Forces HTTPS for all network traffic — no cleartext allowed -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs only (no user-installed certs in production) -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext ONLY for localhost during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!-- Pin our API certificate (add real pins before release) -->
    <!-- Uncomment and add actual SHA-256 pins from your SSL cert:
    <domain-config>
        <domain includeSubdomains="true">api.nipino-manabu.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">YOUR_CERT_PIN_BASE64_HERE==</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_BASE64_HERE==</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
