gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config — contains the project's API key (GitHub secret-scanning
# flagged it in commit 7011fca). Not committed going forward; regenerate
# via `flutterfire configure` or download fresh from the Firebase console.
app/google-services.json
